import { FileManager } from '../components/fileManager'

/**
 * FileManagement Page
 *
 * Enterprise file management for imported observation data.
 * Allows users to view, delete, and replace imported files.
 */
const FileManagement = () => {
  return <FileManager />
}

export default FileManagement
